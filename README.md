# IDS-TCP
A Python-based network monitoring tool for Linux that analyzes TCP traffic in real-time, detecting malicious patterns like the Nimda worm, high packet rates, and blacklisted IPs. It automatically blocks suspicious IPs, logs events, and generates system alerts.
